#ifndef RA_COMPAT_WTYPES_H
#define RA_COMPAT_WTYPES_H

/* Stub for the Win32 header pulled in by RAInterface/RA_Interface.h, so the
 * rest of the tree can be compiled and measured. The types it would supply are
 * declared in msvc_compat.h; a real port replaces the Win32 typing of that
 * interface rather than stubbing it. */

#endif /* RA_COMPAT_WTYPES_H */
